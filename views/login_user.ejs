<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Document</title>
</head>
<body>
    <%- include("partials/header") %>

    <div>
        <h1>Login Page</h1>
        <form action="/users/login" method="post">
            <label for="username">Username</label>
            <input type="text" name="username" placeholder="Enter Username" <% if(locals.data) { %> value='<%= data.username %>' <% } %> ><br>
            <label for="password">Password</label>
            <input type="password" name="password" placeholder="Enter Password" <% if(locals.data) { %> value='<%= data.password %>' <% } %> ><br>
            <input type="submit" value="Login"><br>
            <% if(locals.error) { %>
                <p style="color: red;">
                    <% if (locals.message) { %> 
                        <%= message %> 
                    <% } else { %>
                        <%= 'Incorrect Username and Password' %>
                    <% } %>
                </p>
            <% } %> 
            <a href="/users/create">Don't Have Account</a>
        </form>
    </div>

    <%- include("partials/footer") %> 
</body>
</html>